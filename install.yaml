#ddev-generated
# Details about the install.yaml file are at https://ddev.readthedocs.io/en/stable/users/extend/additional-services/#sections-and-features-of-ddev-get-add-on-installyaml

name: ddev-pantheon-toolkit

# List of files and directories that are copied into project .ddev directory
# Each file should contain #ddev-generated so it can be replaced by a later `ddev get`
project_files:
  - commands

# Version constraint for DDEV
ddev_version_constraint: '>= v1.23.0'

# Post-installation actions
post_install_actions:
  # Make commands executable
  - chmod +x .ddev/commands/web/*

  # Display success message
  - |
    #ddev-description:Pantheon Toolkit installation complete
    echo ""
    echo "✓ DDEV Pantheon Toolkit installed successfully!"
    echo ""
    echo "Available commands:"
    echo "  ddev find-upstreams   - Discover upstreams in your organization"
    echo "  ddev find-people      - List all team members across your sites"
    echo "  ddev add-org          - Add supporting org to sites"
    echo "  ddev site-inventory   - Generate comprehensive site inventory report"
    echo ""
    echo "Important: You must authenticate with Terminus first:"
    echo "  ddev ssh"
    echo "  terminus auth:login"
    echo ""
    echo "Run any command with --help for usage information."
    echo ""
